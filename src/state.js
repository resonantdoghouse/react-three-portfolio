import * as THREE from 'three';

const state = {
  activeMesh: {},
  activeMeshName: 'Capot001_CAR_PAINT_0',
  cameraPos: new THREE.Vector3(0, 0, Math.PI * 2),
  target: new THREE.Vector3(0, 0, 0),
  shouldUpdate: true,
};

export default state;
